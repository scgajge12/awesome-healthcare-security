# コントリビューションガイド

本リポジトリへの追加、修正、指摘を歓迎する。

---

## 何を歓迎するか

| 種別 | 内容 |
|---|---|
| **事例の追加** | 公表された医療機関, 製薬企業のインシデント、医療システムの脆弱性事例 |
| **一次情報へのリンク** | 現在は概要のみ記載している項目に、公表資料へのリンクを追加する |
| **誤りの指摘** | 事実誤認、日付や数値の誤り、誤解を招く表現 |
| **リンク切れの報告** | 参照先の移転、削除 |
| **規制情報の更新** | ガイドラインの改定、新しい規制の施行 |
| **英語版の整備** | 各ページの英訳 |

## 何を掲載しないか

本リポジトリは公開リポジトリである。
次に該当する内容は掲載しない。

- 未公表のインシデント情報、および当事者が公表していない被害の詳細
- 実在する患者の情報、およびそれを含むスクリーンショットやログ
- 特定の組織や個人を非難する記述
- 未修正の脆弱性の詳細（責任ある開示の手続きを経ていないもの）
- そのまま攻撃に使える形の実装コード、エクスプロイト
- 出典を確認できない情報

医療のインシデントには、実在する患者と、対応にあたった医療従事者がいる。
記述は、公表された事実と、そこから学べることに限る。

---

## 文章の規範

本リポジトリの日本語文書は、次の文章規範に従って執筆, 校正する。

**[日本語技術文書の文章規範](skills/japanese-tech-writing/SKILL.md)**

追加, 修正を行う際は、この規範に照らして校正してほしい。
特に次の点は、レビューで確認する。

| 項目 | 内容 |
|---|---|
| 事実と推測の分離 | 「事実」「報道ベース」「分析」を明示的に書き分ける |
| ダッシュを使わない | 日本語の地の文と見出しで `—` `――` を使わない。括弧か句読点で書く |
| 中黒を並列に使わない | 並列は読点で書く。固有名詞の内部での使用は問題ない |
| 太字を多用しない | 論理の要所に限る |
| 空虚な表現を避ける | 「重要なのは〜である」「〜の観点から」「不可欠」「極めて」など、中身を伴わない語を使わない |
| 冗長を排する | 同じ主張を言い換えて繰り返さない |
| 見出しは内容を特定する | 「はじめに」のような情報量のない見出しにしない |

一文ごとに改行し、段落は空行で区切る。

---

## レビュー

Pull Request を出す前に、[レビュースキル](skills/repo-review/SKILL.md)の手順で自己レビューしてほしい。
一次情報、事実と推測の分離、文章規範、公開適格性、図解の五つの観点をまとめている。

## 図解

構造や因果を説明するときは、図を添えてほしい。

- 関係、流れ、階層は **Mermaid** で書く（GitHub がそのまま描画する）
- バイト構造や層の重なりなど、位置関係が意味を持つ図は **SVG** を `assets/` に置き、`<img>` で参照する
- SVG は明色と暗色の両テーマで読めるようにする

## 記述のルール

### 事実と推測を分ける

```markdown
**事実**
- 2021 年 10 月 31 日、電子カルテを含む院内システムが暗号化された。
- 病院が公表した調査報告書で、VPN 装置の脆弱性が侵入経路として指摘されている。

**報道ベース**
- 身代金の要求額は約 ◯◯ とされる（当事者は公表していない）。

**分析**
- 境界機器のパッチ適用が遅れる背景には、閉域網を前提とした設計思想がある。
```

### 一次情報にリンクする

報道記事ではなく、当事者の公表資料、行政文書、CVE、ベンダアドバイザリを優先する。
リンクは、恒久的に残る可能性が高い URL を選ぶ。
PDF への直リンクは移転で切れやすいため、公表元のページを指す方が望ましい。

出典を確認できない記述は、削除するか「未確認」と明記してほしい。
被害人数、金額、期間といった数値は、必ず出所を示す。
CVE 番号を書く場合は、NVD で実在と内容を確認してほしい。

### 攻撃手法は防御とセットで書く

攻撃の説明だけを書かない。
検知方法と緩和策を必ず併記する。

### 帰属を断定しない

攻撃グループの特定は、当事者または公的機関が公表している場合に限って記載する。
それ以外は「未公表」または「報道ベース」と明記する。

---

## 追加の手順

### Issue で提案する

- 情報の追加：[Add a resource](https://github.com/scgajge12/awesome-healthcare-security/issues/new?template=add-resource.yml)
- 誤りの指摘：[Report a correction](https://github.com/scgajge12/awesome-healthcare-security/issues/new?template=correction.yml)

### Pull Request を送る

1. リポジトリをフォークする
2. ブランチを作成する（例：`add/incident-example`）
3. 該当するページを編集する。事例の追加には [`docs/_templates/`](docs/_templates/) のテンプレートを使う
4. 出典を明記する
5. Pull Request を作成する

### コミットメッセージ

Gitmoji と Conventional Commits の形式に従う。

```
<絵文字> <type>(<scope>): <要約>
```

例：

```
📝 docs(incidents): 海外の事例に Synnovis の項目を追加
🐛 fix(guidelines): 医療法施行規則の施行時期の記載を修正
🔗 docs(resources): リンク切れを修正
```

よく使う対応：`docs` は 📝、`fix` は 🐛、`feat` は ✨、リンク修正は 🔗。

---

## ライセンスについて

本リポジトリは [CC BY 4.0](LICENSE) で公開されている。
Pull Request を送ることで、その貢献が同じライセンスで公開されることに同意したものとする。

引用元がある内容を追加する場合は、引用元のライセンスを確認し、必要な帰属表示を記載してほしい。
第三者の文書を全文転載せず、要約とリンクで示す方針を取る。

---

<sub>[トップへ](README.md)</sub>
